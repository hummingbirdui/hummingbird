---
import ContentLayout from '@layouts/ContentLayout.astro';
import { privacyData } from 'apps/data/company';
---

<ContentLayout heading="Hummingbird Privacy Policy" subHeading="Effective Date: 2025">
  {
    privacyData.map((item) => (
      <div class="mb-12">
        <h2 class="text-2xl font-semibold mb-3">{item.title}</h2>
        <p class="text-muted tracking-tight">{item.content}</p>

        {item.subSections && (
          <div>
            {item.subSections.map((sub) => (
              <div>
                <h3 class="text-xl font-medium mt-6 mb-2">{sub.subTitle}</h3>
                <p class="text-muted tracking-tight">{sub.subContent}</p>
              </div>
            ))}
          </div>
        )}
      </div>
    ))
  }
</ContentLayout>
