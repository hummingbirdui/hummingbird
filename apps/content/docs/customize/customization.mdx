---
title: Customization
description: Though Hummingbird works great out of the box, you can customize it to fit your project's unique design needs.
toc: true
---

## Using Utility Classes

Lets dive into how you can customize Hummingbird to better fit your project's design needs. Suppose you want to customize button component:

<button class="btn mb-4">Base Button</button>

```html
<button class="btn">Base Button</button>
```

#### 1. Hummingbird utility
Using Hummingbird's utility classes, you can easily modify the button's appearance. For example you can change it's color, variants, size etc.

<div class='flex flex-wrap gap-2 mb-4'>
  <button class="btn btn-primary">Primary</button>
  <button class="btn btn-subtle-secondary">Secondary</button>
  <button class="btn btn-outline-success">Success</button>
  <button class="btn btn-text-info">Info</button>
</div>
```html
<button class="btn btn-primary">Primary</button>
<button class="btn btn-subtle-secondary">Secondary</button>
<button class="btn btn-outline-success">Success</button>
<button class="btn btn-text-info">Info</button>
```

#### 2. Tailwind utility

You can customize using tailwind utility classes. For example, you can create rounded pill buttons by adding `rounded-full` class, you can all other tailwind utility classes as well.

<div class='flex flex-wrap gap-2 mb-4'>
  <button class="btn btn-primary rounded-full">Pill Button</button>
  <button class="btn btn-subtle-secondary px-12 rounded-full hover:shadow-2xs">Pill Button</button>
</div>
```html
<button class="btn btn-primary rounded-full">Pill Button</button>
<button class="btn btn-subtle-secondary px-12 rounded-full hover:shadow-2xs">Pill Button</button>
```

<h4 class='font-medium'>3. <code class='text-base'>@apply</code> directive</h4>

You can customize inside CSS file using Tailwind CSS `@apply` directive,

```css
@utility btn {
  @apply px-12 rounded-full hover:shadow-2xs;
}
```

## Using CSS variables

Hummingbird components are styled using a set of CSS variables. These variables make it easy to customize styles either globally (across your entire project) or locally (for a specific component).

For example, to update or customize the Button component, you can override its CSS variables:

#### 1. Global variables

Override the theme variables globally under `@theme`. This will apply the changes across all components in your project.

<Example lang='sass' showPreview={false}
  code={`@theme {
    --background-color-highlight: var(--color-gray-100);
    --text-color-default: var(--color-gray-900);
    --color-hover: var(--color-gray-200);
    --color-disabled: var(--color-gray-300);
    --color-disabled-color: var(--color-gray-500);
  }`}/>

#### 2. Local variables

Override the component variables locally under specific components classes or in the custom CSS class. This will apply the changes only to that specific component.

<Example lang='sass' showPreview={false}
  code={`.btn {
    --btn-bg: var(--background-color-highlight);
    --btn-color: var(--text-color-default);
    --btn-hover-bg: var(--color-hover);
    --btn-disabled-bg: var(--color-disabled);
    --btn-disabled-color: var(--color-disabled-color);
  }`}
/>
